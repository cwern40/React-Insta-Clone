import React from 'react';

const Comment = (props) => {
    console.log('Comment', props);
    return (
        <div className="comment-section">
            comments
        </div>
    );
}

export default Comment;